import {screen} from "@testing-library/react";
import {afterEach,beforeEach,describe,expect,it,vi} from "vitest";
import {AppRoutes} from "../routes/AppRoutes";
import {QueryClientProbe,renderWithProviders} from "../test/renderWithProviders";
describe("DevPath frontend scaffold",()=>{beforeEach(()=>vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({error:{code:"AUTHENTICATION_REQUIRED",message:"Authentication is required."}}),{status:401,headers:{"Content-Type":"application/json"}}))));afterEach(()=>vi.unstubAllGlobals());it("renders the Korean application shell",()=>{renderWithProviders(<AppRoutes/>);expect(screen.getByRole("heading",{name:"DevPath"})).toBeInTheDocument();expect(screen.getByRole("status")).toHaveTextContent("DevPath 로그인 상태를 확인하는 중입니다.");});it("renders the Korean not-found route",()=>{renderWithProviders(<AppRoutes/>,["/missing"]);expect(screen.getByRole("heading",{name:"페이지를 찾을 수 없습니다"})).toBeInTheDocument();expect(screen.getByRole("link",{name:"DevPath 홈으로 돌아가기"})).toBeInTheDocument();});it("provides React Query",()=>{renderWithProviders(<QueryClientProbe/>);expect(screen.getByTestId("query-client-present")).toHaveTextContent("true");});});
