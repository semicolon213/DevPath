import {screen} from "@testing-library/react";
import {afterEach,describe,expect,it,vi} from "vitest";
import {AppRoutes} from "../routes/AppRoutes";
import {renderWithProviders} from "../test/renderWithProviders";
const metadata={requestId:"r",apiVersion:"v1",timestamp:"2026-08-25T00:00:00Z"};
describe("LearningRoadmapPage",()=>{afterEach(()=>vi.unstubAllGlobals());it("renders deterministic recommendations and roadmap history",async()=>{vi.stubGlobal("fetch",vi.fn((input:string|URL|Request)=>{const url=String(input);if(url.endsWith("/recommendations/current"))return Promise.resolve(Response.json({data:{recommendationSetId:"rs1",careerReadinessId:"cr1",policyVersion:"recommendation-v1",status:"PUBLISHED",generatedAt:metadata.timestamp,recommendations:[{recommendationId:"rec1",skillGapId:"g1",category:"TESTING",type:"PROJECT",priority:"HIGH",rationaleCode:"CAREER_REQUIRED_GAP",title:"Add testing",completionCriteria:"Reach an official Testing score of at least 60.",expectedEvidence:["test files"],evidenceIds:["e1"],effortHours:16,position:0,status:"PROPOSED"}]},metadata}));if(url.endsWith("/learning-roadmaps")){const roadmap={roadmapId:"rm1",recommendationSetId:"rs1",policyVersion:"roadmap-v1",status:"CREATED",progressPercent:0,milestones:[{milestoneId:"ms1",position:0,category:"TESTING",title:"Add testing",status:"PLANNED"}],steps:[{roadmapStepId:"st1",milestoneId:"ms1",recommendationId:"rec1",position:0,category:"TESTING",title:"Add testing",difficulty:"INTERMEDIATE",effortHours:16,prerequisiteStepIds:[],completionCriteria:"Reach an official Testing score of at least 60.",expectedEvidence:["test files"],status:"NOT_STARTED"}],generatedAt:metadata.timestamp,updatedAt:metadata.timestamp};return Promise.resolve(Response.json({data:{roadmaps:[roadmap]},metadata}));}return Promise.reject(new Error(url));}));renderWithProviders(<AppRoutes/>,["/roadmap"]);expect(await screen.findByRole("heading",{name:"추천 및 학습 로드맵"})).toBeInTheDocument();expect(screen.getAllByText("Add testing")).toHaveLength(2);expect(screen.getByText("높음")).toBeInTheDocument();expect(screen.getByRole("heading",{name:"로드맵 이력"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"현재 로드맵 보관"})).toBeInTheDocument();});});
