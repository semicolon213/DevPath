import {screen} from "@testing-library/react";
import {afterEach,describe,expect,it,vi} from "vitest";
import {renderWithProviders} from "../../../test/renderWithProviders";
import {SessionPanel} from "./SessionPanel";
describe("SessionPanel",()=>{afterEach(()=>vi.unstubAllGlobals());it("renders an anonymous login action after a 401",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(null,{status:401})));renderWithProviders(<SessionPanel/>);expect(await screen.findByRole("link",{name:"GitHub로 로그인"})).toHaveAttribute("href","http://localhost:8080/oauth2/authorization/github");});it("renders the authenticated user and Korean navigation",async()=>{vi.stubGlobal("fetch",vi.fn((input:string|URL|Request)=>{const url=String(input);const data=url.endsWith("/profile")?{profileId:"p",displayName:"DevPath User",careerStage:null,bio:null,updatedAt:"2026-07-27T00:00:00Z"}:url.endsWith("/preferences")?{careerId:null,companyId:null,updatedAt:null}:url.endsWith("/connections")?{connections:[]}:url.endsWith("/careers")?{catalogVersion:"career-v1",careers:[]}:url.endsWith("/companies")?{catalogVersion:"company-v1",companies:[]}:{userId:"u",displayName:"DevPath User",avatarUrl:null,status:"ACTIVE",authenticationProvider:"GITHUB",createdAt:"2026-07-27T00:00:00Z"};return Promise.resolve(Response.json({data,metadata:{requestId:"r",apiVersion:"v1",timestamp:"2026-07-27T00:00:00Z"}}));}));renderWithProviders(<SessionPanel/>);expect(await screen.findByRole("heading",{name:"DevPath User님, 환영합니다"})).toBeInTheDocument();expect(screen.getByRole("link",{name:"내 대시보드"})).toHaveAttribute("href","/dashboard");expect(screen.getByRole("link",{name:"지원 커리어"})).toHaveAttribute("href","/careers");expect(screen.getByRole("button",{name:"로그아웃"})).toBeInTheDocument();});it("distinguishes network failure",async()=>{vi.stubGlobal("fetch",vi.fn().mockRejectedValue(new TypeError("network")));renderWithProviders(<SessionPanel/>);expect(await screen.findByRole("heading",{name:"로그인 상태를 확인할 수 없습니다"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"다시 시도"})).toBeInTheDocument();});});
