# DevPath UI/UX 디자인 요구사항

## 1. 문서 목적

이 문서는 DevPath의 화면을 설계하거나 구현하는 디자이너, 프론트엔드 개발자, AI 에이전트가 공통으로 따라야 하는 UI/UX 기준을 정의한다. 이 문서 하나만 읽어도 제품의 시각적 방향, 정보 우선순위, 화면 상태, 반응형 동작, 접근성 기준을 이해할 수 있어야 한다.

이 문서는 새로운 비즈니스 기능을 정의하지 않는다. 기능 범위는 `docs/01_SRS.md`, 화면 및 프론트엔드 구조는 `docs/12_Frontend_Architecture.md`, API 동작은 `docs/10_API_Specification.md`를 따른다. 충돌이 발생하면 해당 권위 문서가 우선한다.

## 2. 제품 경험 목표

DevPath는 GitHub와 Notion에서 수집한 개발 활동을 근거로 기술 역량과 성장 상태를 보여 주는 개발자 커리어 인텔리전스 플랫폼이다.

사용자는 화면을 통해 다음 질문에 빠르게 답할 수 있어야 한다.

1. 현재 내 개발 역량은 어떤 상태인가?
2. 이 결과는 어떤 근거로 만들어졌는가?
3. 목표 직무와 회사 기준에서 무엇이 부족한가?
4. 다음으로 무엇을 학습하거나 개선해야 하는가?
5. 내 프로젝트 경험을 포트폴리오와 이력서에 어떻게 표현할 수 있는가?

모든 화면은 “점수 표시”보다 “근거 이해와 다음 행동”을 우선한다.

## 3. 핵심 UX 원칙

### 3.1 신뢰 가능한 설명

- 공식 점수와 평가 결과는 Rule Engine 및 Career Engine 결과만 표시한다.
- AI 설명은 공식 평가와 시각적으로 구분하고 `AI 생성` 또는 동등한 표지를 제공한다.
- 점수, 준비도, 추천에는 가능한 경우 근거 보기 또는 출처 연결을 제공한다.
- AI가 점수, 가중치, 준비도, 추천 우선순위를 계산한 것처럼 표현하지 않는다.

### 3.2 행동 중심 정보 구조

- 각 주요 화면은 사용자가 수행할 수 있는 다음 행동을 한 가지 이상 명확히 제시한다.
- 빈 화면은 단순히 “데이터 없음”으로 끝내지 않고 원인과 다음 행동을 함께 안내한다.
- 오류는 실패 사실, 영향 범위, 사용자가 취할 수 있는 조치를 구분하여 보여 준다.

### 3.3 점진적 정보 공개

- 첫 화면에는 요약과 핵심 변화만 보여 주고 상세 근거는 드릴다운으로 제공한다.
- 전문 용어에는 짧은 설명 또는 도움말을 제공한다.
- 복잡한 표와 차트는 필터, 요약, 상세 보기의 순서로 탐색하게 한다.

### 3.4 일관된 상태 표현

- 도메인 생명주기, 비동기 작업 상태, API 값, 화면 표시 상태를 무조건 하나의 enum으로 합치지 않는다.
- 화면은 원본 상태를 사용자에게 이해하기 쉬운 문구로 매핑하되 의미를 변경하지 않는다.
- 색상만으로 성공, 경고, 실패, 준비도를 구분하지 않는다.

### 3.5 사용자 통제권

- 동기화, 분석, 생성, 게시, 삭제처럼 영향이 큰 작업은 현재 상태와 결과를 명확히 안내한다.
- 사용자가 작성한 초안이 사라질 수 있으면 페이지 이동 전에 경고한다.
- 공개 포트폴리오와 비공개 원본 근거의 노출 범위를 분리한다.

## 4. 시각적 방향

### 4.1 디자인 성격

DevPath의 시각적 인상은 다음 단어를 기준으로 한다.

| 지향점 | 적용 기준 |
|---|---|
| 전문적 | 장식보다 구조와 정보 전달을 우선한다. |
| 분석적 | 수치, 변화, 근거의 관계를 명확히 보여 준다. |
| 차분함 | 과도한 그라데이션, 네온, 빛나는 효과를 사용하지 않는다. |
| 성장 지향 | 부족한 점을 비난하지 않고 개선 가능한 다음 단계로 표현한다. |
| 개발자 친화적 | 기술명, 저장소, 근거, 변경 이력을 읽기 쉽게 구성한다. |

### 4.2 금지되는 시각 패턴

- 모든 영역을 동일한 흰색 카드로 감싸는 단조로운 카드 나열.
- 정보 의미 없이 사용하는 유리 효과, 과도한 그림자, 네온 색상.
- 중요한 수치를 크게 보이게 하기 위한 과장된 게이지나 게임식 연출.
- 빨강과 초록만으로 성공과 실패를 구분하는 표현.
- AI 생성 결과와 공식 분석 결과를 같은 형태로 표시하는 구성.
- 모바일에서 가로 스크롤을 강제하는 고정 폭 대시보드.

## 5. 디자인 토큰 요구사항

### 5.1 색상

색상은 역할 기반 토큰으로 관리한다. 컴포넌트에서 임의의 색상 값을 반복 사용하지 않는다.

| 토큰 범주 | 용도 |
|---|---|
| Background | 앱 배경, 표면, 상승 표면의 계층 구분 |
| Text | 기본, 보조, 비활성, 반전 텍스트 |
| Border | 기본 경계, 강조 경계, 입력 포커스 |
| Brand | 주요 행동과 현재 선택 상태 |
| Success | 완료 또는 정상 상태 |
| Warning | 주의가 필요하지만 계속 가능한 상태 |
| Danger | 실패, 파괴적 작업, 중대한 검증 오류 |
| Info | 중립적인 안내와 진행 정보 |
| Evidence | 근거 및 출처 연결의 일관된 강조 |
| AI | AI 생성 콘텐츠 식별용 보조 강조 |

모든 텍스트와 배경 조합은 WCAG 2.2 AA 명도 대비를 충족해야 한다. 브랜드 색상은 공식 점수의 높고 낮음을 의미하지 않는다.

### 5.2 타이포그래피

- 본문은 한국어와 영문 기술 용어를 함께 표시해도 읽기 쉬운 산세리프 계열을 사용한다.
- 페이지마다 `h1`은 하나만 사용하고 제목 계층을 건너뛰지 않는다.
- 숫자 비교가 많은 표와 지표는 자릿수 정렬을 고려한다.
- 코드, 브랜치명, 저장소명, 기술 식별자는 본문과 구분되는 고정폭 스타일을 사용할 수 있다.
- 본문 텍스트는 확대 시 잘리거나 겹치지 않아야 한다.

### 5.3 간격과 크기

- 기본 간격은 일관된 단계형 스케일을 사용한다.
- 클릭 또는 터치 대상은 최소 44×44 CSS 픽셀을 목표로 한다.
- 카드 내부 간격, 섹션 간격, 페이지 여백의 위계를 명확히 구분한다.
- 상태 배지, 아이콘, 보조 텍스트가 핵심 콘텐츠보다 시각적으로 우세하지 않아야 한다.

### 5.4 모서리, 그림자, 움직임

- 모서리 반경은 컴포넌트 계층별로 제한된 토큰만 사용한다.
- 그림자는 떠 있는 메뉴, 모달, 드롭다운처럼 실제 깊이 표현이 필요한 곳에만 사용한다.
- 애니메이션은 상태 변화를 이해시키는 용도로만 사용하며 장식 목적으로 반복하지 않는다.
- `prefers-reduced-motion` 환경에서는 비필수 전환과 움직임을 제거한다.

## 6. 전역 화면 구조

### 6.1 앱 셸

인증된 사용자 화면은 다음 영역으로 구성한다.

1. 전역 탐색 영역
2. 현재 위치와 페이지 제목 영역
3. 페이지 핵심 행동 영역
4. 주요 콘텐츠 영역
5. 필요 시 보조 정보 또는 상세 패널
6. 전역 알림 및 피드백 영역

전역 탐색에는 Dashboard, Repositories, Skills, Recommendations, Roadmap, Knowledge, Artifacts, Portfolio, Resume, Interview, Settings가 포함될 수 있으며 실제 노출은 구현된 기능과 권한에 한정한다. Administration은 일반 사용자 탐색과 분리한다.

### 6.2 탐색 규칙

- 현재 위치를 색상 외에도 형태, 텍스트 또는 접근성 속성으로 식별한다.
- 저장소, 분석, 아티팩트, 포트폴리오, 이력서 화면은 필요한 경우 자원별 탭을 제공한다.
- 모바일에서는 전역 탐색을 접근 가능한 메뉴로 축소하고 현재 페이지 맥락을 유지한다.
- 사용 권한이 없는 링크를 숨기는 것만으로 보안을 구현하지 않는다. 백엔드 권한 검사가 최종 기준이다.

### 6.3 페이지 헤더

페이지 헤더는 다음 순서를 권장한다.

- 브레드크럼 또는 상위 맥락
- 페이지 제목과 짧은 설명
- 상태 또는 마지막 갱신 시간
- 가장 중요한 기본 행동 한 개
- 필요 시 보조 행동 메뉴

동일 중요도의 버튼을 여러 개 강조하지 않는다.

## 7. 핵심 화면 요구사항

### 7.1 로그인 및 세션

- 로그인 화면은 GitHub OAuth2 로그인을 명확한 기본 행동으로 제공한다.
- 브라우저가 공급자 토큰을 직접 받거나 저장하는 것처럼 표현하지 않는다.
- 세션 확인 중에는 로그인/로그아웃 상태가 순간적으로 뒤바뀌지 않도록 중립 로딩 상태를 사용한다.
- `401` 익명 상태와 네트워크 오류 상태를 서로 다른 메시지와 행동으로 표시한다.

### 7.2 온보딩

- 연결 계정, 기본 프로필, 목표 직무, 목표 회사의 진행 상태를 단계별로 보여 준다.
- 목표 회사는 선택 사항일 수 있으므로 선택하지 않았다고 오류처럼 표현하지 않는다.
- 완료되지 않은 단계와 완료된 단계를 텍스트로 구분한다.
- 사용자가 나중에 완료할 수 있는 단계는 건너뛰기 조건을 명확히 안내한다.

### 7.3 대시보드

- 한 화면에서 현재 상태, 최근 변화, 주요 강점, 개선 영역, 다음 행동을 파악할 수 있어야 한다.
- 위젯은 독립적으로 로딩하고 일부 실패가 전체 화면을 막지 않아야 한다.
- 데이터 최신 시점과 분석 기준을 표시한다.
- 차트에는 동일 정보를 이해할 수 있는 텍스트 요약 또는 표 대안을 제공한다.
- 빈 대시보드는 저장소 연결이나 분석 시작 등 다음 행동을 제시한다.

### 7.4 저장소 목록 및 상세

- 목록은 저장소명, 주요 기술, 공개 여부, 마지막 동기화 시점, 동기화 상태를 쉽게 비교하게 한다.
- 데스크톱은 표 또는 밀도 높은 목록, 모바일은 카드 또는 요약 목록으로 전환할 수 있다.
- 동기화, 권한 오류, 보관 상태는 색상과 상태 문구를 함께 사용한다.
- 저장소 상세에서는 메타데이터, 스냅샷, 분석 결과, 근거를 구분하여 보여 준다.
- 저장소 동기화 상태와 저장소 자체의 도메인 상태를 혼동하지 않는다.

### 7.5 분석 결과와 Skill Matrix

- 공식 점수에는 점수명, 값, 계산 버전 또는 기준, 근거 접근 수단을 함께 제공한다.
- Skill Matrix는 기술명, 역량 수준, 신뢰도, 근거 상태를 구분한다.
- 매트릭스 시각화에는 스크린 리더와 모바일 사용자를 위한 목록 또는 표 대안을 제공한다.
- 강점과 약점은 낙인 표현 대신 관찰된 근거와 개선 방향으로 설명한다.
- 데이터가 부족한 경우 낮은 점수와 동일하게 표현하지 않고 `근거 부족` 상태로 구분한다.

### 7.6 Career 및 Company Readiness

- 현재 선택된 목표 직무와 회사를 화면 상단에서 확인할 수 있어야 한다.
- 직무 준비도와 회사 준비도를 서로 다른 개념으로 표시한다.
- 목표 변경 시 기존 결과가 어떤 영향을 받는지 안내하고 필요한 경우 재평가 상태를 표시한다.
- 준비도는 색상만이 아니라 숫자, 라벨, 설명을 함께 제공한다.

### 7.7 추천과 Learning Roadmap

- 추천은 행동, 근거, 기대 효과, 우선순위의 출처를 구분하여 표시한다.
- AI 설명이 포함되면 공식 추천 정책과 분리하여 표기한다.
- 로드맵은 단계, 상태, 예상 기간, 학습 목표, 완료 조건을 보여 준다.
- 완료, 건너뛰기, 메모 변경은 낙관적 표시를 사용하더라도 서버 실패 시 되돌리고 이유를 안내한다.
- 타임라인은 키보드와 모바일에서도 순서대로 탐색할 수 있어야 한다.

### 7.8 Knowledge

- 지식 문서는 출처, 버전, 갱신 시점, 처리 상태를 표시한다.
- 검색 결과는 관련도뿐 아니라 원문 출처로 이동할 수 있어야 한다.
- 오래되었거나 일부 인덱싱이 실패한 자료는 최신 자료와 구분한다.
- Knowledge 시스템은 답변 생성기가 아니라 검색 근거 제공 계층임을 UI 문구에서 혼동하지 않는다.

### 7.9 AI 생성

- 생성 전에는 작업 유형, 사용할 근거 범위, 결과 형식을 확인할 수 있어야 한다.
- 생성 중에는 일반 비동기 작업 상태를 사용하되 작업 단계가 제공되면 보조 설명으로 표시한다.
- 결과에는 `AI 생성`, 생성 시각, 사용한 근거, 검증 또는 경고 상태를 표시한다.
- 생성 실패는 공급자 내부 정보 대신 안전한 설명과 재시도 가능 여부를 제공한다.
- AI 결과를 공식 점수나 평가 결과처럼 시각화하지 않는다.

### 7.10 Portfolio, Resume, README 및 Interview

- 생성 초안과 사용자가 직접 수정한 내용을 구분할 수 있어야 한다.
- 편집 화면은 미리보기, 저장 상태, 버전, 내보내기 상태를 제공한다.
- 공개 포트폴리오에는 내부 `sourceRefs`, 비공개 저장소 정보, 원문 근거 식별자를 노출하지 않는다.
- 공개 전에는 공개 범위와 포함 콘텐츠를 확인하는 단계를 제공한다.
- 인터뷰 질문은 질문, 관련 기술 또는 역량, 난이도, 답변/피드백 상태를 구분한다.
- README 개선안은 원본과 제안 결과를 비교할 수 있어야 하며 자동으로 저장소에 반영된 것처럼 표현하지 않는다.

### 7.11 설정 및 관리

- 연결 상태, 개인정보 설정, 목표 직무/회사, 계정 비활성화 또는 삭제 요청을 명확히 구분한다.
- 파괴적 행동은 일반 저장 행동과 시각적으로 분리하고 확인 단계를 제공한다.
- 관리자 화면은 일반 사용자 작업 공간과 분리하며 관리자 권한을 백엔드 응답으로 확인한다.

## 8. 공통 컴포넌트 요구사항

| 컴포넌트 | 요구사항 |
|---|---|
| Button | 기본, 보조, 위험, 텍스트 행동을 구분하고 로딩 중 중복 실행을 방지한다. |
| Input | 항상 연결된 라벨, 도움말, 오류 메시지를 제공한다. |
| Select/Combobox | 키보드 탐색, 검색 가능 여부, 선택 상태를 명확히 지원한다. |
| Card | 관련 정보만 그룹화하고 모든 콘텐츠를 습관적으로 카드로 만들지 않는다. |
| Table | 헤더, 정렬 상태, 필터, 빈 상태, 모바일 대안을 제공한다. |
| Badge | 상태 보조 표현으로 사용하며 배지만으로 의미를 전달하지 않는다. |
| Dialog | 초점 가두기, 닫기, 제목, 설명, 복귀 초점을 지원한다. |
| Toast | 짧은 성공 알림에 사용하며 중요한 오류를 토스트만으로 전달하지 않는다. |
| Alert/Banner | 지속적인 경고, 부분 장애, 보안 또는 데이터 상태를 표시한다. |
| Skeleton | 최종 레이아웃을 예측할 수 있을 때 사용하고 무기한 표시하지 않는다. |
| Chart | 제목, 단위, 범례, 기간, 텍스트 요약, 데이터 표 대안을 제공한다. |
| Evidence Link | 근거 유형과 출처를 식별할 수 있는 텍스트를 사용한다. |
| AI Label | AI 생성 영역에 일관되게 표시하고 공식 결과와 구분한다. |

## 9. 화면 상태 요구사항

모든 서버 데이터 기반 화면은 해당되는 상태를 명시적으로 설계한다.

| 상태 | 표현 원칙 |
|---|---|
| Initial Loading | 구조를 예측할 수 있으면 스켈레톤, 진행 단계가 중요하면 진행 표시를 사용한다. |
| Refreshing | 기존 데이터를 유지하고 작은 갱신 표시를 사용한다. |
| Empty | 비어 있는 이유와 가능한 다음 행동을 제공한다. |
| Partial | 사용 가능한 데이터는 유지하고 실패한 영역만 별도로 표시한다. |
| Success | 결과와 최신 시점, 다음 행동을 제공한다. |
| Validation Error | 필드 오류와 상단 요약을 함께 제공하고 첫 오류로 초점을 이동한다. |
| Authorization Error | 안전한 문구로 접근 불가를 안내하며 존재 여부를 과도하게 노출하지 않는다. |
| Provider Error | GitHub, Notion, LLM 내부 세부 정보 없이 영향과 재시도 방법을 안내한다. |
| Internal Error | 일반 오류 메시지와 요청 식별자를 제공한다. |
| Conflict | 최신 데이터 다시 불러오기, 변경 비교, 재시도 방법을 제공한다. |

## 10. 비동기 작업 UX

공통 비동기 작업 화면 상태는 `QUEUED`, `RUNNING`, `SUCCEEDED`, `FAILED`, `CANCELLED`와 같은 API 표현을 사용자 문구로 매핑할 수 있다. 저장소, 분석, 지식 문서, 생성 아티팩트의 도메인 생명주기는 이 공통 작업 상태와 별도로 표시한다.

- 진행 중에는 작업 대상과 현재 단계 또는 경과 상태를 보여 준다.
- 페이지를 벗어나도 작업이 계속되는지 명확히 안내한다.
- 취소와 재시도는 API가 허용하는 경우에만 제공한다.
- 성공 시 결과 화면으로 이동할 수 있는 명확한 행동을 제공한다.
- 실패 시 재시도 가능 여부와 이미 완료된 부분의 영향을 설명한다.
- 진행률을 계산할 수 없는 경우 임의의 백분율을 만들지 않는다.

## 11. 반응형 요구사항

디자인 검수 기준 화면 폭은 다음 범주를 사용한다. 정확한 CSS 중단점은 디자인 토큰에서 관리한다.

| 범주 | 기본 동작 |
|---|---|
| Mobile | 단일 열, 접이식 탐색, 요약 우선, 표는 카드/목록 대안 제공 |
| Tablet | 1~2열, 접이식 보조 패널, 주요 행동 유지 |
| Desktop | 지속 탐색, 다중 열 대시보드, 상세 비교와 보조 패널 지원 |

- 핵심 기능은 모바일에서도 가로 스크롤 없이 사용할 수 있어야 한다.
- 데이터 표처럼 가로 구조가 필수인 영역은 스크롤 사실과 고정 헤더/열 사용 여부를 명확히 한다.
- 차트는 모바일에서 요약을 먼저 보여 주고 상세 차트를 펼칠 수 있게 한다.
- 에디터는 모바일에서 편집과 미리보기를 탭 또는 순차 영역으로 분리한다.
- 화면 방향 변경과 200% 확대에서도 핵심 행동이 가려지지 않아야 한다.

## 12. 접근성 요구사항

- 목표 적합성 기준은 WCAG 2.2 AA이다.
- 모든 기능은 키보드만으로 수행할 수 있어야 한다.
- 포커스 표시를 제거하지 않으며 라우트 이동, 모달, 오류, 비동기 완료 후 포커스를 예측 가능하게 관리한다.
- 상태 변화와 작업 완료는 필요한 경우 라이브 영역으로 전달한다.
- 이미지, 아이콘, 차트에는 목적에 맞는 대체 텍스트 또는 텍스트 대안을 제공한다.
- 폼은 시각적 placeholder가 아닌 명시적 라벨을 사용한다.
- 오류 문구는 문제와 수정 방법을 구체적으로 설명한다.
- 상태와 점수는 색상 외에 텍스트, 아이콘 형태, 패턴 중 하나 이상을 함께 사용한다.
- 언어가 혼합되는 기술명과 고유명사는 읽기 순서와 발음을 방해하지 않게 구성한다.
- 자동 접근성 검사만으로 완료를 판단하지 않고 키보드와 스크린 리더 핵심 흐름을 수동 검수한다.

## 13. 콘텐츠 및 문체

### 13.1 기본 문체

- 짧고 직접적인 존댓말을 사용한다.
- 사용자를 평가하거나 비난하는 표현 대신 관찰된 상태와 개선 행동을 제시한다.
- 버튼은 `확인`, `처리` 같은 모호한 단어보다 `저장소 연결`, `분석 시작`, `초안 생성`처럼 결과가 드러나는 동사를 사용한다.
- 오류 메시지에 내부 예외명, SQL, 공급자 응답 원문, 스택 트레이스를 노출하지 않는다.

### 13.2 용어 구분

| 용어 | UI 표현 원칙 |
|---|---|
| Repository | Git 저장소 단위이며 Project와 동일시하지 않는다. |
| Project | 하나 이상의 저장소와 문서를 포함할 수 있는 사용자 프로젝트 맥락이다. |
| Technology | 언어, 프레임워크, 데이터베이스, 도구 등 관찰 가능한 기술이다. |
| Skill | 근거와 평가를 통해 표현되는 개발 역량이다. |
| Evidence | 평가의 출처이며 Score 자체가 아니다. |
| Recommendation | 정책에 따른 권고이며 AI 답변과 동일하지 않다. |
| AI Response | 구조화된 근거를 설명하거나 초안을 생성한 결과이다. |
| Generated Artifact | 생성 작업의 결과물이며 작업 상태와 별도 생명주기를 가진다. |

## 14. 개인정보 및 보안 UX

- 비공개 저장소, Notion 문서, 근거 데이터의 공개 범위를 기본적으로 비공개로 취급한다.
- OAuth 공급자 토큰, 세션 ID, 내부 권한 정보는 화면과 클라이언트 로그에 표시하지 않는다.
- 외부 연결 해제, 계정 비활성화, 삭제 요청은 영향과 처리 범위를 사전에 안내한다.
- 공개 포트폴리오 미리보기와 소유자 편집 화면을 명확히 분리한다.
- 권한이 없는 자원의 존재 여부를 추측할 수 있는 상세 오류를 제공하지 않는다.
- 민감한 작업 완료 메시지는 결과를 확인할 수 있어야 하며 토스트만으로 끝내지 않는다.

## 15. 성능 체감 요구사항

- 앱 셸과 현재 세션 확인을 우선 표시하고 무거운 차트, 에디터, 관리자, AI 도구는 지연 로딩한다.
- 대시보드는 전체 응답을 기다리지 않고 위젯별로 표시할 수 있어야 한다.
- 라우트 변경 시 더 이상 필요하지 않은 요청은 안전한 범위에서 취소한다.
- 사용자 입력에는 즉각적인 시각 피드백을 제공하되 서버 성공을 미리 확정해 표시하지 않는다.
- 장시간 작업은 빈 로딩 화면 대신 대상, 상태, 취소/이탈 영향 정보를 제공한다.

## 16. 측정 가능한 UI/UX 요구사항

| ID | 요구사항 | 검증 기준 |
|---|---|---|
| UX-001 | 모든 주요 화면은 로딩, 빈 상태, 성공, 오류를 정의해야 한다. | 라우트별 상태 테스트 또는 스토리에서 4개 상태 확인 |
| UX-002 | 부분 데이터가 가능한 화면은 부분 실패를 전체 실패와 분리해야 한다. | 대시보드 및 복합 상세 화면에서 정상 영역 유지 확인 |
| UX-003 | 공식 결과와 AI 생성 설명을 시각·텍스트로 구분해야 한다. | AI 영역에 일관된 표지와 설명 존재 |
| UX-004 | 점수와 준비도는 근거 또는 상세 화면으로 연결되어야 한다. | 핵심 지표에서 키보드로 근거 접근 가능 |
| UX-005 | 차트와 Skill Matrix는 비시각적 대안을 제공해야 한다. | 동일 핵심 정보를 텍스트 또는 표로 확인 가능 |
| UX-006 | 모든 인터랙티브 요소는 키보드로 조작 가능해야 한다. | 핵심 사용자 흐름에서 마우스 없이 완료 가능 |
| UX-007 | 상태 표현은 색상에만 의존하지 않아야 한다. | 모든 상태에 텍스트 또는 추가 시각 기호 존재 |
| UX-008 | 폼 오류는 필드와 요약에서 확인 가능해야 한다. | 제출 실패 후 오류 요약 및 첫 오류 초점 확인 |
| UX-009 | 모바일 핵심 흐름은 불필요한 가로 스크롤 없이 동작해야 한다. | 로그인, 대시보드, 저장소, 분석, 로드맵 흐름 검수 |
| UX-010 | 사용자 초안 손실 가능 시 이탈 경고를 제공해야 한다. | 변경 후 라우트 이탈 시 경고 확인 |
| UX-011 | 중요한 오류는 토스트만으로 전달하지 않아야 한다. | 페이지 또는 컴포넌트에 지속 오류 메시지 존재 |
| UX-012 | 공개 화면은 내부 근거 식별자와 비공개 원문을 노출하지 않아야 한다. | 공개 포트폴리오 응답 및 화면 점검 |
| UX-013 | 비동기 진행률을 알 수 없을 때 임의 백분율을 표시하지 않아야 한다. | 불확정 작업은 단계/활동 표시 사용 |
| UX-014 | `401`과 네트워크 장애를 다른 사용자 상태로 표시해야 한다. | 세션 부트스트랩 테스트에서 문구와 행동 분리 |
| UX-015 | 주요 페이지는 200% 확대에서도 기능과 콘텐츠를 유지해야 한다. | 확대 후 겹침, 잘림, 기능 손실 없음 |
| UX-016 | 화면 텍스트와 배경은 WCAG 2.2 AA 대비를 충족해야 한다. | 자동 대비 검사와 핵심 화면 수동 검수 통과 |
| UX-017 | 비동기 완료 및 오류는 필요한 경우 보조기술에 전달되어야 한다. | 라이브 영역 또는 적절한 초점 이동 확인 |
| UX-018 | 파괴적 작업은 영향 안내와 확인 단계를 제공해야 한다. | 연결 해제, 보관, 삭제 흐름 검수 |

## 17. 구현 및 리뷰 체크리스트

### 17.1 구현 전

- 화면이 연결되는 요구사항 ID와 API가 확인되었는가?
- 아직 구현되지 않은 기능을 화면만으로 실제 기능처럼 보이게 만들지 않았는가?
- 공식 결과, 도메인 상태, 비동기 작업 상태, AI 결과를 구분했는가?
- 데스크톱, 태블릿, 모바일 정보 우선순위를 정의했는가?
- 로딩, 빈 상태, 부분 성공, 오류, 권한 없음 상태를 정의했는가?

### 17.2 구현 중

- 기존 디자인 토큰과 공통 컴포넌트를 재사용했는가?
- React Query 서버 상태를 별도 전역 상태로 복제하지 않았는가?
- 입력 라벨, 키보드 탐색, 포커스, 상태 문구를 함께 구현했는가?
- 민감한 데이터가 화면, URL, 브라우저 저장소, 로그에 노출되지 않는가?
- 컴포넌트가 공식 점수나 비즈니스 규칙을 계산하지 않는가?

### 17.3 리뷰 전

- 핵심 사용자 흐름을 키보드만으로 완료할 수 있는가?
- 모바일과 200% 확대에서 잘림이나 기능 손실이 없는가?
- 자동 접근성 검사와 수동 검수를 모두 수행했는가?
- 모든 실패 상태가 사용자의 다음 행동을 설명하는가?
- 차트와 수치가 최신 시점, 단위, 기준, 근거를 제공하는가?
- AI 생성 영역이 공식 분석과 명확히 구분되는가?
- 테스트하지 못한 상태를 완료로 보고하지 않았는가?

## 18. 디자인 변경 관리

- 새로운 비즈니스 화면은 SRS 요구사항과 API 계약 없이 추가하지 않는다.
- 디자인 토큰, 전역 탐색, 공통 상태 패턴 변경은 개별 기능에서 임의로 결정하지 않는다.
- 접근성 또는 보안 기준을 낮추는 예외는 허용하지 않는다.
- 실제 구현 증거가 없는 화면은 `계획`, `초안`, `프로토타입` 상태로 명확히 표시한다.
- 이 문서의 요구사항이 권위 문서와 충돌하면 구현을 멈추고 충돌을 기록한 뒤 상위 문서를 기준으로 해결한다.
