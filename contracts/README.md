# DevPath Contracts

This directory owns machine-readable contract artifacts.

Current scope:

- `openapi/devpath-openapi.yaml` contains only the scaffold health endpoint.
- Business APIs remain specified in `docs/10_API_Specification.md` until implemented as machine-readable contracts.

